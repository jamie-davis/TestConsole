Normal adapter:                                             
-->                                                         
----+----|----+----|----+----|----+----|----+----|----+----|
Long console output that should require word wrapping to fit
the display width. This was split into many small writes<-- 
                                                            
Prefixed adapter:                                           
-->                                                         
prefix: ----+----|----+----|----+----|----+----|----+----|--
prefix: Long console output that should require word        
prefix: wrapping to fit the display width. This was split   
prefix: into many small writes<--                           